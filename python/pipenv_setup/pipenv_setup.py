"""
open terminal in folder, type pipenv --three (sets python3 as the version for that environment)
dev-packages are local packages to make it work on your own computer
packages are dependencies that must be present at all times and must be pushed up with the files

use the command "pipenv shell"

install packages locally in the pipenv through the command "pipenv install numpy" or whatever the library is called

command to open repl is still python
"""